<% for(var i = 0; i < users.length; i++) { %>
<tr>
    <td><%= i + 1 %></td>
    <td><%= users[i].username %></td>
    <td><%= users[i].fullname %></td>
    <td><%= users[i].age %></td>
    
    <td>
       
      
        <a class="btn border-shadow delete" data-id=<%= users[i].username%> >
            <span class="text-gradient"><i class="fas fa-times"></i></span>
        </a>
    </td>
</tr>
<% } %>